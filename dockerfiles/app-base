# DEV
FROM python:3.6

WORKDIR /mass_speck
RUN git clone https://github.com/MatteoLacki/masstodon.git
RUN cd masstodon; git checkout devel; pip3 install -e .
# RUN cd masstodonnewtests/ReadingJson
# RUN json2masstodon masstodon_input.json --plot
# pip2 install MassTodonPy # change to
